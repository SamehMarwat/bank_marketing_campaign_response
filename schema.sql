CREATE TABLE bank_marketing (
    age INTEGER,
    job VARCHAR(50),
    marital VARCHAR(20),
    education VARCHAR(50),
    default_status VARCHAR(10),
    housing VARCHAR(10),
    loan VARCHAR(10),
    contact VARCHAR(20),
    month VARCHAR(10),
    day_of_week VARCHAR(10),
    duration INTEGER,
    campaign INTEGER,
    pdays INTEGER,
    previous INTEGER,
    poutcome VARCHAR(20),
    emp_var_rate DECIMAL(8,3),
    cons_price_idx DECIMAL(8,3),
    cons_conf_idx DECIMAL(8,3),
    euribor3m DECIMAL(8,3),
    nr_employed DECIMAL(10,3),
    subscribed VARCHAR(10)
);

